import React from 'react';
import AppTitle from '../app-title';
import './header.css';

const Header = () => {
  return(
  <div className='header'>
    <AppTitle />
  </div>
  );
};

export default Header;
